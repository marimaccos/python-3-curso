# aula importando módulos
# apertando Ctrl+space aparece os módulos e funcionalidades que podems er importados

import math

num = int(input('Digite um número: '))
raiz = math.sqrt(num)

print('A raiz de {} é {:.2f}.'.format(num, raiz))
#print('A raiz de {} é {}.'.format(num, math.ceil(raiz)))
